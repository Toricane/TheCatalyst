# requirements.txt
fastapi
uvicorn[standard]
python-dotenv
litellm
pydantic
python-multipart
aiofiles
SQLAlchemy
toons

# To install and run:
# 1. pip install -r requirements.txt
# 2. python app.py
# 3. Open frontend/index.html in browser (or serve it with any local server)
